<nav class="navbar navbar-default navbar-static-top navbar-white" role="navigation">
  <div class="container">
    <%= link_to image_tag("ziada_nav_brand.png", alt: "Ziada"), home_index_url, class: 'navbar-brand' %>    
    <button class="navbar-toggle" data-toggle="collapse" data-target=".navHeaderCollapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <div class="collapse navbar-collapse navHeaderCollapse">
      <ul class="nav navbar-nav navbar-right">
        <li class="<%= yield(:home) %>"><%= link_to "Home", home_index_url %></li>
        <li class="<%= yield(:products) %>"><%= link_to "Products", products_url %></li>
        <li class="<%= yield(:categories) %>"><%= link_to "Categories", categories_url %> </li>

        <% if logged_in? %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= current_user.name %><span class="caret"></span></a>
            <ul class="dropdown-menu">
              <li><%= link_to 'Admin', admin_root_url %></li>
              <li><%= link_to 'Profile', user_url(current_user) %></li>
              <li><%= link_to 'Logout', logout_url, method: :delete %></li>
            </ul>
          </li>        
        <% end %>
      </ul>
    </div>
  </div>
</nav>
