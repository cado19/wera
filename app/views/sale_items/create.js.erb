<% if @sale_item.errors.any? %>
  alert("Invalid")
<% else %>
  $('.cart-text').html("<%= j render('layouts/shared/cart_text') %>")
  $('.cart-item').html("<%= j render('carts/cart_item') %>")
  $('.subtotal').html("<%= j render('carts/cart_total') %>")
<% end %>

// This partial deals with creation of sale Items
// After a sale item is saved in the database,
// 1. The Number of products in the cart increases. So the cart text is re rendered using jquery
// 2. A cart item is added to the cart.
// 3. The cart total changes. The new new cart total is re rendered.
