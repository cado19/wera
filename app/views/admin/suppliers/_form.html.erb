<%= bootstrap_form_for [:admin, @supplier] do |f| %>
  <% if @supplier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supplier.errors.count, "error") %> prohibited this supplier from being saved:</h2>

      <ul>
      <% @supplier.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.text_field :telephone %>
  </div>

  <div class="field">
    <%= f.text_field :email %>
  </div>

  <div class="actions">
    <%= f.button class: 'btn btn-primary btn-sm', data: {disable_with: "<i class='fa fa-spinner fa-spin'></i> Saving Details..."} %>
  </div>
<% end %>
