<%= form_tag password_resests_url, method: :post do %>
	<%= label_tag :email %>
	<%= text_field_tag :email, class: 'form-control' %>

	<%= submit_tag "Reset Password", class: 'btn btn-primary' %>
<% end %>