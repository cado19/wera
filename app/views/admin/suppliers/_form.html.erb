<%= bootstrap_form_for [:admin, @supplier] do |f| %>
  <% if supplier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(supplier.errors.count, "error") %> prohibited this supplier from being saved:</h2>

      <ul>
      <% supplier.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name, id: :supplier_name %>
  </div>

  <div class="field">
    <%= f.text_field :telephone, id: :supplier_Telephone %>
  </div>

  <div class="field">
    <%= f.text_field :email, id: :supplier_email %>
  </div>

  <div class="actions">
    <%= f.submit class: 'btn btn-primary btn-sm' %>
  </div>
<% end %>
