<% title "Ziada | Customers" %>
<h3 align="center">Customers</h3>
<br>

<section class="content">
  <div class="box box-primary">
    <div class="box box-body">
      <div class="pull-right">
        <button type="button" class="btn btn-info btn-sm" data-toggle="modal" data-target="#myModal">
          <i class="fa fa-search"></i> Search Customer
        </button>
      </div>

      <table class="table table-condensed table-responsive">
        <thead>
          <tr>
            <th><%= sort_link(@search, :name) %></th>
            <th><%= sort_link(@search, :contact) %></th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @customers.each do |customer| %>
            <tr>
              <td><%= customer.name %></td>
              <td><%= customer.contact %></td>
              <td><%= link_to 'Details', admin_customer_url(customer) %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </box>
  </box>
</section>
<br>

<div class="modal fade" id="myModal" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">

      <!-- Modal Header -->
      <div class="modal-header">
        <h4 class="modal-title">Search Customer</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div> <!-- /.modal-header -->

      <!-- Modal Body -->
      <div class="modal-body">
        <div class="row">
          <div class="container">
            <%= search_form_for @search, url: search_admin_customers_url do |f| %>
              <p>
                <%= f.label :name_cont, "Name" %>
                <%= f.text_field :name_cont %><br>
              </p>

              <p>
                <%= f.label :contact_cont, "Contact" %>
                <%= f.text_field :contact_cont %><br>
              </p>

              <p>
                <%= f.submit class: 'btn btn-sm' %>
              </p>
            <% end %>
          </div>
        </div>
      </div><!-- /.modal-body -->
      <div class="modal-footer">
          <button type="button" class="btn btn-default" data-dismiss="modal">Cancel</button>
        </div>
    </div><!-- -->
  </div>
</div><!-- -->
